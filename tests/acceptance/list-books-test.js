import { module, test } from 'qunit';
import { visit, currentURL } from '@ember/test-helpers';
import { setupApplicationTest } from 'ember-qunit';

module('Acceptance | list books', function(hooks) {
  setupApplicationTest(hooks);

  test('should show AllBooks as the home page', async function (assert) {
  });

  test('should link to external site.', async function (assert) {
  });

  test('should link to selections information.', async function (assert) {
  });

  test('should list books.', async function (assert) {
  });

  test('should filter the list of books by tags.', async function (assert) {
  });

  test('should show details for a selected book', async function (assert) {
  });
});
